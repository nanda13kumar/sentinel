# Sentinel Backend — Environment Variables
# Copy to .env and fill in values

# Required for AI analysis (get from console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: point to real Prometheus (leave blank for mock data)
PROMETHEUS_URL=

# Server config
HOST=0.0.0.0
PORT=8000
